package pl.remonty.remonty.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pl.remonty.remonty.model.RenovationCrew;

public interface RenovationCrewRepository extends JpaRepository<RenovationCrew, Long> {
}
