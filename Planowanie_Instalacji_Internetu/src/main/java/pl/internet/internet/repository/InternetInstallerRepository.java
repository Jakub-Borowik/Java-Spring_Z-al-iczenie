package pl.internet.internet.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pl.internet.internet.model.InternetInstaller;

public interface InternetInstallerRepository extends JpaRepository<InternetInstaller, Long> {
}
